#name "Process quick check"
#version "1.0.0"
#mode safe
#strict
#cap proc.list proc.modules runtime.print

let target = "powershell"
var pid = proc.find(target)

print("{target} pid={pid}")
print("Loaded modules for pid={pid}:")
proc.modules(pid)
proc.list()
